[build]
  publish = "dist"
  command = "npx convex deploy && npm run build"

[build.environment]
  # VITE_CONVEX_URL will be set by Convex integration or manually in Netlify UI

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

